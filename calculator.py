def add(x, y):
    return x + y

def subtract(x, y):
    return x - y

def multiply(x, y):
    return x * y

def divide(x, y):
    if y == 0:
        return ""
    return x /y




    # Indexing: Accessing individual elements using indices.
    # Slicing: Extracting sub-sequences using [start:end:step] notation.
    # Concatenation (+): Combining sequences.
    # Repetition (*): Repeating sequences.
    # Membership (in, not in): Checking if an element exists in the sequence.
    # Length (len()): Getting the number of elements in the sequence.
    # Iterating (for loop): Iterating over each element in the sequence.
    # Min (min()), Max (max()): Finding the minimum and maximum elements.
    # Appending (append()): Adding an element at the end (only for mutable sequences like lists).
    # Extending (extend()): Adding elements from another sequence at the end (only for mutable sequences like lists).
    # Inserting (insert()): Inserting an element at a specified position (only for mutable sequences like lists).
    # Removing (remove()): Removing an element by value (only for mutable sequences like lists).
    # Counting (count()): Counting occurrences of an element.
    # Index (index()): Finding the index of the first occurrence of an element.



